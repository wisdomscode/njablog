import 'package:flutter/material.dart';

class ApiPostsScreen extends StatelessWidget {
  const ApiPostsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Api Posts')),
    );
  }
}
